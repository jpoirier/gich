Description
------------
    Giche  a cross platform which tool written in Go

    A which utility takes a list of command names and searches the path for each executable file that would be run had
    these commands actually been invoked.

Compiling
---------
    make

Testing
-------
    make test

Usage
-----
    giche [-l|-s|-h|-help] file ...
        -l  List all executable instances found rather than just the first one.
        -s  Output 'Found' if any of the executables were found and 'None' if none were found
        -h  Print this usage message
        -help  Print this usage message


